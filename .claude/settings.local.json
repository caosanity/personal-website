{
  "permissions": {
    "allow": [
      "Bash(powershell.exe -Command \"if \\(Get-Command node -ErrorAction SilentlyContinue\\) { node --version } else { Write-Output ''Node not found'' }\")"
    ]
  }
}
